# DG News MERN Project

A MERN stack application for a news website, based on https://dgnews.co.in/.

## Setup
1. Install dependencies in both `frontend/`: `npm install`.
2. Run frontend: `npm run dev` in `frontend/`.
3. Visit http://localhost:5173.

## Features
- Responsive home page with TailwindCSS.